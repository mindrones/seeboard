{
  "name": "seeboard",
  "description": "A keyboard activity visualiser",
  "author": "Luca Bonavita - mindrones.com",
  "version": "0.0.1",
  "scripts": {
    "newdir": "rm -rf build data && mkdir build data",
    "devbuild": "npm run newdir && gulp build",
    "prodbuild": "npm run newdir && gulp build --production",
    "dev": "npm run newdir && gulp",
    "prod": "npm run newdir && gulp --production",
    "deploy": "npm run newdir && gulp deploy --production",
    "test": "jest"
  },
  "license": "MIT",
  "dependencies": {
    "lamb": "^0.55.0",
    "word-list": "^2.0.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "browser-sync": "^2.24.6",
    "gh-pages": "^1.2.0",
    "gulp": "^4.0.0",
    "jest": "^23.5.0",
    "minimist": "^1.2.0",
    "rollup": "^0.63.2",
    "rollup-plugin-alias": "^1.4.0",
    "rollup-plugin-buble": "^0.19.2",
    "rollup-plugin-commonjs": "^9.1.3",
    "rollup-plugin-json": "^3.0.0",
    "rollup-plugin-node-resolve": "^3.0.3",
    "rollup-plugin-svelte": "^4.0.0",
    "rollup-plugin-terser": "^1.0.1",
    "svelte": "^2.6.0"
  }
}
